import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
// next.config.js
module.exports = {
  reactStrictMode: true,
  // Enable additional Next.js configurations here if needed
}

